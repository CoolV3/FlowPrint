import LoginForm from "@/componens/auth/loginForm";

export default function LoginPage() {

    return(
        <div className="flex items-center justify-center">
            <LoginForm/>
        </div>
    )
}